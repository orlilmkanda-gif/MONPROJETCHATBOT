// index.js
// Point d'entrée pour lancer le serveur MWALIMU

const app = require('./server.js'); // on importe le serveur depuis server.js

// Lancement du serveur sur le port 3000
app.listen(3000, () => {
  console.log('Serveur MWALIMU lancé via index.js sur http://localhost:3000');
});
